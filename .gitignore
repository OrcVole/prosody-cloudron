data/*
tests/certs/
tests/venv/
tests/__pycache__/

# Secret hygiene — never commit tokens/credentials/certs
.env
*.token
*.secret
.cloudron*
*.pem
*.key

# The anonymity denylist names what must never be published, so it is never published itself.
.anonymize-list
